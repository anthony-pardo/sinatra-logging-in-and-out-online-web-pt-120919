<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if is_logged_in?(session) %>
      <h1>User Data:</h1>
      <%= current_user(session).username %>
      <%= current_user(session).balance %>
      <a href="/logout">Log Out</a>
    <% else %>
      <a href="/">Home</a>
    <% end %>
  </body>
</html>